apiVersion: 1

datasources:
  - name: StacjaPogodowaSQLite
    type: frser-sqlite-datasource
    access: proxy
    isDefault: true
    jsonData:
      path: /var/lib/grafana/sensor_data.db
    editable: true